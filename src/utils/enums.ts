export enum LinkRoutes {
  BASE = "/",
  HOME = "/home",
  LOGIN = "/login",
  CUSTOMER = "/customer",
  ADMIN = "/admin",
  SUPER = "/super",
  DASHBOARD = "/dashboard",
  UNAUTHORIZED = "/unauthorized",
}
